// IINA Plugin Bookmarks - Entry Point
// This file is only used to reference the self-contained bundle
// The actual plugin code is in plugin-bundle.ts

// Load the plugin bundle (this will be compiled to JavaScript)
import './plugin-bundle';